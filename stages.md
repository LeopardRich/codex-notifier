# codex-notifier 实施阶段

本文把 `README.md` 和 `README-zh.md` 中的目标架构拆分为 20 个按依赖排序的
实施阶段。除非某阶段的全部检测标准均满足，否则不得把该阶段标记为完成，也不应
开始依赖它的后续阶段。

通用完成门槛：

- 所有新增行为都有自动化测试，平台专属行为另有对应平台的真实环境验证记录。
- 从第 3 阶段建立工具链后，每个阶段都必须通过 `cargo fmt --all -- --check`、
  `cargo clippy --workspace --all-targets --all-features -- -D warnings` 和
  `cargo test --workspace`。
- 行为、协议、支持平台或安装方式发生变化时，中英文 README 必须同步更新。
- 未执行的检测项必须标记为“未验证”，不能用代码审查或模拟测试代替真实平台测试。

## 阶段 01：确认 Codex 外部事件能力

目标：确认目标 Codex CLI 版本是否能把任务完成和等待审批事件交给外部程序。

交付物：

- `docs/compatibility.md` 中记录目标版本、操作系统、Codex 使用界面和事件能力矩阵。
- `tests/fixtures/` 中保存经过脱敏的真实事件载荷及其来源版本说明。
- 对不支持的事件给出明确降级行为，禁止默认读取会话日志或抓取终端输出。

检测标准：

- 在每个目标 Codex 版本上分别触发一次任务完成和一次审批请求。
- 每类标记为“支持”的事件都有真实外部进程调用证据和脱敏载荷样本。
- 每类“不支持”或“待确认”的事件在兼容表中有明确状态，程序规划不依赖猜测字段。
- 载荷样本不包含提示词、模型输出、用户名、密钥、绝对路径或其他机器信息。

## 阶段 02：冻结架构决策与安全边界

目标：在编码前解决会改变公开行为、协议或发布方式的关键决策。

交付物：

- ADR 覆盖最低 Codex/Windows/macOS 版本、许可证、通知隐私默认值、SSH 拓扑和发布渠道。
- `docs/threat-model.md` 完成资产、信任边界、攻击面和缓解措施分析。
- 规范事件协议的首版字段、大小限制和兼容策略得到确认。

检测标准：

- 所有 README“待定架构决策”都有 Accepted ADR，或有负责人和截止条件。
- 威胁模型覆盖本地 IPC、SQLite、SSH、日志、系统通知和安装器六个边界。
- 明确记录首版不提供通知内审批按钮、不开放 HTTP 端口、不嵌入 SSH 服务端。
- 中英文 README 与 ADR 结论无冲突。

## 阶段 03：建立 Rust workspace 与质量门禁

目标：创建可重复构建的空工作区、工具链约束和基础 CI。

交付物：

- 根 `Cargo.toml`、固定 Rust 工具链、统一格式化与 lint 配置。
- README 中规划的各 crate 和应用 package 正式加入 workspace。
- Windows、macOS、Linux relay 三类 CI 基础任务。

检测标准：

- 全新检出后不依赖未声明的全局 Rust package 即可执行构建。
- `cargo metadata --no-deps` 成功且 workspace 成员与架构目录完全一致。
- 格式化、Clippy 严格警告和 workspace 测试命令均以退出码 0 完成。
- CI 在三类目标 runner 上至少完成一次绿色运行。

## 阶段 04：实现规范事件领域模型

目标：实现与 Codex、SSH、IPC 和操作系统无关的事件类型与校验规则。

交付物：

- `schema_version`、UUIDv7 `event_id`、事件类型、时间、来源、展示与路由模型。
- 字段长度、时间偏移、扩展字段、总载荷大小和未知版本策略。
- 稳定的序列化契约和错误分类。

检测标准：

- 两类目标事件都能完成序列化往返且不丢字段。
- 对未知必需版本、未知事件类型、超长字段、超大载荷和非法时间分别有拒绝测试。
- 属性测试或等价的边界测试覆盖所有数值与长度上下界。
- `core` 的依赖树不包含 Codex、SSH、SQLite 或平台通知库。

## 阶段 05：实现分层配置与路径规则

目标：实现跨平台、一致且不会泄漏机器配置的配置系统。

交付物：

- 内置默认值、用户配置、配置档和 CLI 覆盖项的确定性合并。
- Windows、macOS 和 XDG 用户配置/状态路径解析。
- 配置版本迁移、敏感字段策略和可读错误信息。

检测标准：

- 自动化测试证明四层配置的优先级和覆盖行为符合 README。
- 三个平台路径测试不依赖运行测试的主机实际用户名或目录。
- 未知必需配置、非法角色、非法端点和不可写状态目录分别产生可识别错误。
- 配置文件与日志中不会出现私钥正文或事件原始载荷。

## 阶段 06：建立结构化日志与隐私策略

目标：在后续适配器加入前固定可观测性字段和默认脱敏行为。

交付物：

- 统一事件状态、错误码、耗时和关联 ID 日志模型。
- 默认脱敏、日志轮转和安全诊断输出策略。
- 测试使用的内存日志接收器。

检测标准：

- 日志快照只包含事件 ID、类型、状态和安全错误，不包含展示正文。
- 包含换行、控制字符、终端转义和伪造字段的输入不能污染日志结构。
- 日志级别变化不会绕过敏感字段脱敏。
- 轮转和保留边界有确定性测试。

## 阶段 07：实现 SQLite 发件箱与去重存储

目标：提供崩溃安全的至少一次队列和桌面端投递记录。

交付物：

- 事务性入队、租约、确认、重试、死信和去重接口。
- 数据库 schema 版本与向前迁移机制。
- 队列大小、事件年龄和记录保留策略。

检测标准：

- 进程在入队、发送中和确认前被中止后，未确认事件仍可恢复。
- 相同 `event_id` 重复提交不会产生第二次有效投递。
- 队列满、数据库锁定、磁盘不可写和迁移失败均返回分类错误。
- 迁移测试能从所有已发布 schema 样本升级且不会丢失待发事件。

## 阶段 08：实现本地 IPC 传输

目标：让 `emit`、`receive` 与 agent 通过仅当前用户可访问的本地通道通信。

交付物：

- Windows 命名管道和 macOS/Linux Unix domain socket 适配器。
- 有界帧协议、请求确认、超时和 agent 身份校验。
- IPC 端点创建、清理和陈旧端点恢复逻辑。

检测标准：

- 合法事件能在所有目标平台完成提交与结构化确认。
- 非当前用户、超大帧、截断帧、慢速发送和并发连接测试均按策略处理。
- agent 异常退出后可以恢复端点，且不会连接到其他用户的残留端点。
- 本地 IPC 测试确认不读取也不使用 HTTP/HTTPS 代理变量。

## 阶段 09：实现 agent 生命周期与路由

目标：组合配置、IPC、存储与端口接口，形成稳定的用户级 agent。

交付物：

- `desktop` 与 `relay` 显式角色路由。
- 启动、就绪、排空、优雅关闭和单实例控制。
- 有界 worker、背压与取消策略。

检测标准：

- `desktop` 角色不初始化 SSH，`relay` 角色不初始化系统通知 API。
- 第二个同配置档 agent 启动时得到明确的“已运行”结果。
- 关闭期间停止接收新事件，进行中事件被确认或安全退回队列。
- 压力测试达到规定并发量时，内存和任务数量保持在配置上限内。

## 阶段 10：接入 Codex 任务完成事件

目标：把已验证版本的 Codex 任务完成载荷转换为规范事件。

交付物：

- 按 Codex 版本隔离的任务完成事件源适配器。
- `emit` 的任务完成接入路径和兼容性错误。
- 脱敏映射规则与 fixture 驱动测试。

检测标准：

- 阶段 01 的每个支持版本 fixture 都能产生预期规范事件快照。
- 缺字段、字段类型变化、未知版本和多余敏感字段都有测试。
- 从 Codex 调用到 agent 接收的本地集成测试在规定超时内返回。
- 规范事件不包含原始提示词、回复、环境变量或绝对工作目录。

## 阶段 11：接入 Codex 审批请求事件

目标：在目标 Codex 版本确实暴露外部事件时支持等待审批通知。

交付物：

- 审批请求事件源适配器和与任务完成事件一致的隐私映射。
- 不支持该事件时的能力报告和安装期提示。
- 明确不具备远程批准能力的展示文案。

检测标准：

- 每个标记支持的 Codex 版本都通过一次真实审批等待触发测试。
- 不支持的版本不会安装无效 hook，也不会启动日志或终端抓取器。
- 通知载荷不包含待执行命令、敏感参数或可触发审批的动作 URL。
- 兼容表、`doctor` 结果和实际适配器选择保持一致。

## 阶段 12：实现 Windows 原生通知适配器

目标：在交互式 Windows 用户会话中可靠显示系统 Toast。

交付物：

- Windows 通知 API 适配器、应用身份处理和权限诊断。
- 两类事件的标题、正文、紧急程度与隐私级别映射。
- Windows 平台安装与卸载所需资源清单。

检测标准：

- 在最低和最新支持 Windows 版本上分别显示两类真实 Toast。
- 通知关闭、勿扰、缺失应用身份和非交互会话都有明确诊断结果。
- 载荷中的 XML/控制字符/超长文本不能破坏通知或产生注入。
- 非 Windows 构建不链接或初始化 Windows 通知实现。

## 阶段 13：实现 macOS 原生通知适配器

目标：通过 macOS 原生通知框架显示系统通知。

交付物：

- macOS 通知适配器、应用包身份和授权状态诊断。
- 与 Windows 一致的事件展示语义和隐私映射。
- 签名、公证和 LaunchAgent 所需资源清单。

检测标准：

- 在最低和最新支持 macOS 版本上分别显示两类真实通知。
- 首次授权、拒绝授权、勿扰和无 GUI 会话都有明确诊断结果。
- 特殊 Unicode、控制字符和超长文本通过安全截断测试。
- 非 macOS 构建不链接或初始化 macOS 通知实现。

## 阶段 14：完成桌面端本地使用闭环

目标：让本地 Codex 用户可以安装、运行、检查、测试并完整卸载桌面端。

交付物：

- `install`、`uninstall`、`agent`、`status` 和本地 `test` 命令行为。
- Codex 集成与用户级自启动产物的所有权记录。
- 幂等安装、升级和回滚流程。

检测标准：

- 全新用户环境中一次安装后，本地任务完成事件能产生原生通知。
- 重复安装不产生重复 hook、重复自启动项或不同配置副本。
- 卸载只移除本工具拥有的配置，并保留用户未授权删除的数据。
- Windows 与 macOS 都有安装、重启登录会话、升级和卸载的真实记录。

## 阶段 15：实现受限 SSH 接收入口

目标：建立远程输入到桌面 agent 的最小可信入口。

交付物：

- `receive` 单事件 stdin 协议、大小限制、校验和结构化确认。
- 专用 SSH 密钥的 forced-command 配置模板与安全说明。
- 主机密钥登记和授权文件权限诊断。

检测标准：

- 合法事件经真实 OpenSSH 会话到达桌面 agent 并收到确认。
- 任意命令参数、PTY、端口转发、Shell 请求和多事件拼接均被拒绝。
- Shell 元字符只作为数据处理，不能改变命令、路径或通知动作。
- 错误确认不回显完整载荷、路径、密钥或内部堆栈。

## 阶段 16：实现 relay SSH 发件箱投递

目标：让远程 agent 使用系统 OpenSSH 可靠地转发排队事件。

交付物：

- 参数化 OpenSSH 进程适配器、主机别名和超时策略。
- 指数退避、随机抖动、确认处理和永久错误分类。
- SSH 不可用期间的队列状态与恢复行为。

检测标准：

- SSH 进程只使用参数数组，事件信封仅通过 stdin 发送。
- 断网、连接超时、认证失败、主机密钥变化和远端拒绝分别有测试。
- 短暂故障恢复后事件按至少一次语义送达，桌面端只展示一次。
- 达到重试年龄或次数上限的事件按策略进入死信且不无限占用资源。

## 阶段 17：实现诊断、状态与端到端测试命令

目标：让用户不查看敏感日志也能定位配置和投递问题。

交付物：

- `doctor`、`status` 和远程 `test` 的稳定输出与退出码。
- Codex 能力、agent、IPC、存储、通知权限、OpenSSH 和目标连通性检查。
- 面向用户的分层修复建议。

检测标准：

- 每个已知故障类别都有独立错误码、退出码和不泄密的修复建议。
- `doctor` 默认只读，不修改 Codex、SSH、启动项或通知设置。
- 人类可读输出与机器可读输出表达相同状态。
- 诊断快照测试证明不包含事件正文、用户名、私钥或完整机器路径。

## 阶段 18：完成本地与远程端到端可靠性验证

目标：验证四条核心路径及其主要故障恢复行为。

交付物：

- Windows 本地、macOS 本地、远程到 Windows、远程到 macOS 的测试矩阵。
- 崩溃恢复、重复投递、离线重试、队列满和版本不兼容场景报告。
- 性能与资源上限基线。

检测标准：

- 四条核心路径都用真实 Codex、真实 IPC、真实 SSH 和真实通知 API 验证。
- agent 在入队后、发送中和确认前崩溃的三种场景均不丢失事件。
- 100 次重复重试只产生一次用户可见通知。
- 在约定负载下，hook 返回时间、通知延迟、内存和数据库大小均满足 ADR 上限。

## 阶段 19：完成打包、签名、CI 与升级验证

目标：产生可验证来源、可升级、可卸载的跨平台发布产物。

交付物：

- Windows、macOS 和 Linux relay 安装包或归档。
- Windows 签名、macOS 签名与公证流程，以及校验和与软件物料清单。
- 跨平台 CI、依赖审计和前一版本升级测试。

检测标准：

- 每个产物在干净目标系统上安装、启动、自测、升级和卸载成功。
- 签名、公证、校验和与发布版本一致且可由独立命令验证。
- CI 对格式、Clippy、测试、依赖漏洞、许可证和构建产物全部设置阻断门禁。
- 从前一稳定版本升级后，配置和未投递队列仍可读取。

## 阶段 20：执行发布候选审计

目标：证明首个版本满足范围、安全、兼容性和文档承诺。

交付物：

- 发布检查表、已知限制、兼容表、威胁模型复审和回滚方案。
- 最终中英文安装、配置、远程连接、诊断和卸载文档。
- 带版本的发布产物与可复现测试记录。

检测标准：

- 阶段 01 至 19 的证据链接完整，不存在跳过但标记完成的检测项。
- README 中每项支持声明都能追溯到目标平台的真实测试记录。
- 安全审查确认无密钥、凭据、原始提示词、模型输出或机器专属配置进入仓库或产物。
- 在发布候选产物上重新通过四条核心路径和完整卸载测试。
- 所有阻断级问题关闭后才能创建正式版本标签。
